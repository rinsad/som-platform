# SOM Platform frontend environment example
# Copy to .env.development for local development if needed.

# In normal Vite dev mode, keep this as /api so requests go through the Vite proxy.
VITE_API_URL=/api

# Backend target used by vite.config.js for /api proxying.
VITE_PROXY_TARGET=http://127.0.0.1:5000
